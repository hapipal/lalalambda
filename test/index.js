'use strict';

// Load modules

const Lab = require('lab');
const Code = require('code');
const Lalalambda = require('..');

// Test shortcuts

const { describe, it } = exports.lab = Lab.script();
const { expect } = Code;

describe('Lalalambda', () => null);
